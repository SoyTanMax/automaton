export type Collection = {
    collectionId?: string,
    coverImage?: string,
    collectionName?: string,
    floor?: string,
    volume?: string,
    description?: string,
    nftItemIds?: string[]
}